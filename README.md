# mega-itmo
Мегашкола ИТМО 2026, Трек "Coding Agents"

Автоматизированная агентная система для полного цикла разработки (SDLC) в GitHub.

## Возможности

- 🤖 **Code Agent** — анализирует Issue, генерирует код, создаёт Pull Request
- 🔍 **Review Agent** — проверяет код, анализирует CI/CD, публикует ревью
- 🔄 **Итеративные исправления** — автоматически исправляет код по замечаниям
- 🐳 **Docker** — запуск одной командой

## Быстрый старт

### Локально
```bash
# Клонируем репозиторий
git clone https://github.com/YOUR_USERNAME/ai-sdlc-agent.git
cd ai-sdlc-agent

# Создаём окружение
python3.11 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Настраиваем переменные
cp .env.example .env
# Заполни .env своими ключами

# Запускаем Code Agent
python -m src.code_agent.cli --repo owner/repo --issue 1
```

### Docker
```bash
# Настраиваем переменные
cp .env.example .env
# Заполни .env своими ключами

# Запускаем
docker-compose up code-agent
```

## Структура проекта
```
├── src/
│   ├── code_agent/     # Агент для написания кода
│   ├── review_agent/   # Агент для code review
│   └── utils/          # Общие утилиты
├── tests/              # Тесты
├── .github/workflows/  # GitHub Actions
├── Dockerfile
└── docker-compose.yml
```

## Требования

- Python 3.11+
- GitHub Personal Access Token
- OpenAI API Key (или YandexGPT)

## Лицензия

MIT